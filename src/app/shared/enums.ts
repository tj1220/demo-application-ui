export enum screenWidths {
  xs = 360, // for mobile
  sm = 576, // for mobile
  md = 768, // for tab-vertical
  lg = 992, // for tab-horizontal
  xl = 1200 // for PC
};

export enum departments {
  Engineering = `Engineering`,
  Sales = `Sales`,
  HR = `HR`,
  Management = `Management`,
  Accounts = `Accounts`
};

export enum gender {
  Male = `Male`,
  Female = `Female`
};

export enum pages {
  dashboard = `Dashboard`,
  demoItems = `Demo Items`
};
